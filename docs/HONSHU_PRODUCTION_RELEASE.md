# 本州国境線の本番反映（2026-09-10）

ユーザーの編集JSON（SHA-256: ddbe57b84a911d8a0543d515adbc4038c90b93795317b954e28483645570dbac）を基準に、近畿以東の41領域の境界を確定。
近接端点を統合し、交点で線を分割して面を構築した。端点の最大移動量は0.291483ゲーム座標単位。
数値計算由来の1e-6ゲーム座標単位²未満の穴のみ除去し、描画形状の大きな変更や国名に合わせた分割は行っていない。

- 正本：`data/master/political/honshu/1.0.0/`
- 入力・点の移動記録：正本の `approved_input_edits.json`、`cleanup_report.json`
- 承認記録とファイルハッシュ：`political_master_manifest.json`
- 既存の国名データとの対応が不明な11領域：`name_review.json`。境界は確定済み、表示名は「名称未確定」。
- 本番派生：`data/derived/political/approved_western/`。九州・四国・中国地方と合計65領域。
- 中国地方側の共有線は変更せず、4つの区間参照を使って近畿側の選択輪郭を描く。
- 海岸は既存正本の距離区間参照から描画。画像由来の海岸は採用しない。
- 本番版には編集画面と草稿を含めない。旧編集ファイルは履歴として保持。

再生成：`python tools/publish_western_political.py`

検証：65領域のクリック・5段階LOD・画面外非表示、全領域の選択線と枠の一致、共有区間の参照をGodotで確認。
`tests/base_map/godot/test_political_integration.gd` と `test_approved_honshu.gd` を使用。
配布PCKの読込と実行ファイルの起動も確認する。

Windows ZIP：`builds/MyWorldsOnFreedom-Windows-HonshuApproved-v1.0.0.zip`

## 国名照合版 v1.0.1
提示された令制国地図により陸奥・羽後・陸中・陸前・羽前・磐城・岩代・安房の8国名を補完。北部の細い領域、上野・下総を含む領域、摂津・和泉を含む領域の3領域は名称未確定として赤色で表示。全境界座標と領域IDは維持。新正本は honshu/1.0.1。照合根拠は name_resolution.json、保留理由は name_review.json に記録。

## 領域15分割版 v1.0.2
ユーザー指定により領域15の最狭接続部（長さ4.23911ゲーム座標単位）に共有線を追加し、西側を上野国（kozuke）、東側を下総国（shimosa）に分割。旧領域の外周・合計面積・他領域は維持。全66領域、未確定の赤色領域は2。正本 honshu/1.0.2/split_report.json に分割位置と選択確認点を記録。

## 陸奥国統合版 v1.0.3
ユーザー指定により領域02を陸奥国（honshu-area-01）に統合。共通の内部境界を削除し、隣接国との境界参照を陸奥国へ付け替えた。統合前後の領域の合計形状と他国は維持。全65領域、赤色の名称未確定領域は領域36のみ。正本 honshu/1.0.3/merge_report.json に記録。

## 領域36編集版
本番マップに領域36専用編集画面へのボタンを追加。水色の直線追加、外周吸着、範囲削除、Undo/Redo、自動保存、region36_border_edits.jsonの入出力に対応。草稿の保存先は本番レジストリのSHA-256で分離。境界正本v1.0.3は維持。配布PCKで編集・保存復元と本番選択を検証。配布ZIPはRegion36Editor版。

## 摂津・和泉確定版 v1.0.4
region36_border_edits.json の1本の線を採用。端点の浮動小数点誤差を外周に合わせ、北を摂津国、南を和泉国として分割した。他国と海岸線を維持し、全66領域の輪郭と選択を検証。未確定領域は0。領域36の編集UIは終了。入力と分割記録は正本1.0.4に保存。
